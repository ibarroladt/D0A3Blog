<% provide(:title, "#{@post.title}") %>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h1><%= @post.title %></h1>
      <span class="line"></span>
      <hr>
    </div>
  </div>
</div>