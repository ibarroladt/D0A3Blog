<% provide(:title, "Artículos") %>
<div class="container">
  <div class="row">
    <div class="col-md-12">
      <% if @posts.empty? %>
        <h1>No hay artículos en este momento</h1>
        <span class="line"></span>
        <hr>
      <% else %>
        <h1>Artículos</h1>
        <span class="line"></span>
        <hr>
      <% end %>
    </div>
  </div>
</div>